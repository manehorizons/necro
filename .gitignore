node_modules/
dist/
*.log
.DS_Store
HANDOFF.md.resumed-*

# Local secrets — never commit the real .env (template is .env.example)
.env
.env.*
!.env.example

# Local scratch (eval/run output dumps)
dumpfile

# Local CADENCE session artifacts (handoff archive + intelligence context)
.cadence/handoff/
.cadence/intelligence/
