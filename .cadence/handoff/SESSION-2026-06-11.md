---
cadence_handoff: 1
generated_at: 2026-06-11T17:53:09.061Z
label: 
loop_position: IDLE
active_phase: 21-ci-pr-citizen
active_draft: 
tier: 
git_branch: main
git_dirty: true
git_head: 8523c15
git_ahead: 0
git_behind: 0
context_packet: .cadence/intelligence/context/handoff.json
---

# Session Handoff — 2026-06-11

## TL;DR for the next session
- **The whole "ship & adopt" milestone shipped end-to-end this session.** necro is published on npm and is now a CI/PR citizen. Loop is **IDLE**, nothing in flight.
- **`@manehorizons/necro` is live:** `1.0.0` (manual publish) → `1.0.1` (proved the automated pipeline) → `1.1.0` (SARIF + GitHub Action + `--fail-on`). All carry npm **provenance** via the automated tag→build→publish workflow.
- **Both phases settled PASS:** `20-20` (AC-1 publish) and `21-21` (AC-2 CI citizen). Milestone `mil-grp-ship-adopt` is marked **closed** in the (gitignored) intelligence ledger.
- **Clean tree:** everything substantive is committed and pushed (HEAD `8523c15`, 0 ahead). The only "dirty" is `.cadence/` loop state, which is intentionally untracked. Full suite **310 passed**.
- **Next is a strategic choice, not a pending task** — pick among the remaining recs: 006 (necro bench / measured accuracy), 007 (deepen agent wedge / `necro explain`), 008 (false-positive-reduction plugins).

## State on handoff   ·  pre-filled — verify, don't retype
- Branch `main` (dirty), 0 ahead / 0 behind origin
- HEAD `8523c15`
- Recent commits:
```
8523c15 ci: bump codeql-action/upload-sarif to v4 in the necro Action
b5c4f2b release: @manehorizons/necro@1.1.0 — CI/PR citizen (SARIF + Action + --fail-on)
6b1f89a feat(21-ci-pr-citizen): SARIF + GitHub Action + --fail-on gating (AC-2)
a27b65b chore: gitignore docs/launch (local pre-launch planning docs)
4d96d4b ci: bump actions/checkout + actions/setup-node to v5
9b5e310 release: @manehorizons/necro@1.0.1 — CI pipeline smoke test
7cd3df0 release: @manehorizons/necro@1.0.0 — first npm publish
d0b1aa7 docs: synchronize README + docs site with shipped surface (phases 12-19)
```
- Uncommitted (diff --stat):
```
.cadence/STATE.md   | 4 ++--
 .cadence/state.json | 6 +++---
 2 files changed, 5 insertions(+), 5 deletions(-)
```
- Loop: IDLE · phase 21-ci-pr-citizen · tier (none)

## CADENCE context   ·  pre-filled from `cadence context handoff`
- Top recommendations:
  - rec-20260610-004 — Publish @manehorizons/necro to npm + agent-install story (unblocks the MCP server) (accepted/ready-for-milestone)
  - rec-20260610-005 — CI/PR citizen: real SARIF output + GitHub Action + --fail-on gating (accepted/ready-for-milestone)
  - rec-20260610-006 — Public measured-accuracy benchmark (necro bench) — weaponize the eval harness (candidate/needs-decision)
  - rec-20260610-008 — False-positive reduction: Next.js/NestJS plugins + monorepo workspace edges (candidate/needs-decision)
  - rec-20260610-007 — Deepen the agent wedge: necro_verify enhancements + necro explain (candidate/needs-decision)
- Open assumptions:
  - (none)
- Active decisions:
  - (none)
- Files in play:
  - `package.json` — affected by rec-20260610-004 Publish @manehorizons/necro to npm + agent-install story (unblocks the MCP server)
  - `.github/workflows/release.yml` — affected by rec-20260610-004 Publish @manehorizons/necro to npm + agent-install story (unblocks the MCP server)
  - `README.md` — affected by rec-20260610-004 Publish @manehorizons/necro to npm + agent-install story (unblocks the MCP server)
  - `website/src/content/docs/guide/installation.md` — affected by rec-20260610-004 Publish @manehorizons/necro to npm + agent-install story (unblocks the MCP server)
  - `src/cli.ts` — affected by rec-20260610-004 Publish @manehorizons/necro to npm + agent-install story (unblocks the MCP server)
  - `src/report/sarif.ts` — affected by rec-20260610-005 CI/PR citizen: real SARIF output + GitHub Action + --fail-on gating
  - `.github/actions/necro/action.yml` — affected by rec-20260610-005 CI/PR citizen: real SARIF output + GitHub Action + --fail-on gating
  - `website/src/content/docs/guide/ci-integration.md` — affected by rec-20260610-005 CI/PR citizen: real SARIF output + GitHub Action + --fail-on gating
  - `test/fixtures/refactor-dup-realrepo/` — affected by rec-20260610-006 Public measured-accuracy benchmark (necro bench) — weaponize the eval harness
  - `src/bench/` — affected by rec-20260610-006 Public measured-accuracy benchmark (necro bench) — weaponize the eval harness
  - `website/src/content/docs/guide/accuracy.md` — affected by rec-20260610-006 Public measured-accuracy benchmark (necro bench) — weaponize the eval harness
  - `src/plugins/` — affected by rec-20260610-008 False-positive reduction: Next.js/NestJS plugins + monorepo workspace edges
  - `src/engine/index.ts` — affected by rec-20260610-008 False-positive reduction: Next.js/NestJS plugins + monorepo workspace edges
  - `src/graph/` — affected by rec-20260610-008 False-positive reduction: Next.js/NestJS plugins + monorepo workspace edges
  - `src/plugins/registry.ts` — affected by rec-20260610-008 False-positive reduction: Next.js/NestJS plugins + monorepo workspace edges
  - `src/mcp/tools/verify.ts` — affected by rec-20260610-007 Deepen the agent wedge: necro_verify enhancements + necro explain
  - `src/mcp/tools/explain.ts` — affected by rec-20260610-007 Deepen the agent wedge: necro_verify enhancements + necro explain
  - `src/refactor/verify.ts` — affected by rec-20260610-007 Deepen the agent wedge: necro_verify enhancements + necro explain
  - `src/analyze/` — affected by rec-20260610-007 Deepen the agent wedge: necro_verify enhancements + necro explain

## What landed this session
- Launch decisions D1–D5 recorded in `docs/launch/00`; npm scope chosen `@manehorizons/necro` (bare `necro` was taken; renamed off the `@necrotool` placeholder everywhere, incl. the intelligence ledger).
- `1.0.0` manual publish; agent-install verified clean-room (`npx … mcp` MCP handshake + `tools/list`).
- Automated release workflow `.github/workflows/release.yml` (tag→guard→build→test→`npm publish --access public --provenance`); `NPM_TOKEN` repo secret set; `1.0.1` smoke release proved it.
- AC-2 built TDD: `src/report/severity.ts` (Conservative high/med/low), `src/report/sarif.ts` (SARIF 2.1.0), `scan --sarif <file>` + `--fail-on <high|medium|low>`, composite `.github/actions/necro/action.yml` + dogfood workflow. 18 new tests.
- `1.1.0` published; dogfood Action validated **live** (uploads SARIF to code-scanning); `upload-sarif` bumped to v4, `checkout`/`setup-node` to v5 — all CI annotations cleared.
- Version sourced from `package.json` via `src/version.ts` (esbuild inlines at build) so `--version` / MCP identity never drift.
- Docs flipped to npm/npx + the CI surface (README + Astro site), Astro link-check passes; `docs/launch/` gitignored.
- Milestone `mil-grp-ship-adopt` marked **closed**; `cadence intelligence audit` clean.

## Carry-forward gotchas
- **`.cadence/` is intentionally uncommitted** — `STATE.md`/`state.json` modified, phase dirs 15–21 untracked (repo convention). `.cadence/handoff/` and `.cadence/intelligence/` are **gitignored**. Do NOT commit `.cadence/`. No WIP source is uncommitted; nothing was stashed.
- **Releasing:** `npm version <x>` (bumps package.json) → commit → `git tag v<x> && git push origin main --tags`. The workflow **guards `tag == package.json version`** — never push a tag whose version doesn't match, or the publish fails by design.
- **Version is build-time-inlined** from `package.json` (`src/version.ts`). Do NOT re-hardcode a version string in `src/cli.ts` or `src/mcp/server.ts`.
- **necro self-scan is degenerate** (entry points resolve to `dist/`), so the dogfood workflow `.github/workflows/necro-scan.yml` runs `fail-on: ''` (surface-only) on PR/`workflow_dispatch` only — NOT on push. Don't turn on `--fail-on` for necro's own scan.
- **Recs 004/005 still read `accepted`** in the ledger — there is no "done" rec status; completion is recorded only via `mil-grp-ship-adopt` = closed. Don't be confused they aren't "done."
- **`git tag mvp-feature-complete`** preserves the old June-8 `v1.0.0` marker; `v1.0.0` was force-repointed to the npm release commit (`7cd3df0`).
- **`.cadence/ROADMAP.md` is an unused empty stub** ("no phases yet") — phases live in `.cadence/phases/`, milestones in the intelligence ledger. Offered to populate it; user hasn't asked.
- Docs site needs **Node ≥ 22** (`nvm use 22` for `astro build`); this shell defaults to Node 20.

## Next action
**Action:** No work in flight — loop IDLE, ship-adopt milestone shipped and closed. The next move is the user's strategic pick among the remaining recommendations: **rec-006** (necro bench / public measured-accuracy), **rec-007** (deepen the agent wedge — `necro_verify` enhancements + `necro explain`), **rec-008** (false-positive reduction — Next.js/NestJS plugins + monorepo workspace edges). Start by running `cadence recommend` to re-rank, then either `cadence milestone propose` to shape one, or `cadence draft new <slug> <n>` to plan a phase directly.
**Verify:** `cadence status` → IDLE · phase 21-ci-pr-citizen; `cadence milestone list` → `mil-grp-ship-adopt` under **Closed**; `npm view @manehorizons/necro version` → `1.1.0`; `git log origin/main..HEAD` empty (all pushed).
**If it fails:** nothing is pending to recover — the session ended clean. If a deprecation re-surfaces in CI, the only known one left is upstream `codeql-action` v3→v4 (already bumped) / Node-20 runner forced switch on 2026-06-16 (workflows already on `@v5`).
