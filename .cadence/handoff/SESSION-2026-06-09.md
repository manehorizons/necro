---
cadence_handoff: 1
generated_at: 2026-06-09T03:05:18.442Z
label: 
loop_position: IDLE
active_phase: 11-real-repo-triage-eval
active_draft: 
tier: 
git_branch: main
git_dirty: true
git_head: 25b306d
git_ahead: 0
git_behind: 0
context_packet: .cadence/intelligence/context/handoff.json
---

# Session Handoff — 2026-06-09

## TL;DR for the next session
- **Two milestones shipped & pushed** this session; `origin/main` @ `25b306d`, **0 ahead / 0 behind**. Loop **IDLE**, no active draft. `v1.0.0` tagged at the MVP commit.
- **Phase 10** (`extract-duplicate`) and **Phase 11** (`real-repo-triage-eval`) both settled — **all ACs PASS** (7/7 and 6/6).
- **Headline finding (Phase 11):** the real-repo eval revealed `necro triage` is **poor on real findings — precision 0.50–0.75, recall 0.40–0.60** on hono's `maybe` set. The pre-existing synthetic eval (near-perfect) **masked this entirely**. This is the milestone's payoff.
- **Single next action:** scope a **triage tuning phase** (phase 12) to raise precision toward ≥0.85 — phase 11 only *measured*, its boundary forbade tuning. Corpus + harness are ready. (Refactor real-repo eval is also pending.)
- **No blockers, no WIP code.** `247 tests pass / 4 skipped` (the 4 opt-in live evals); typecheck clean. Live evals need a key: `set -a; . ./.env; set +a; npx vitest run test/triage-eval.live.test.ts`.
- Dirty tree = **CADENCE bookkeeping only** (`STATE.md`/`state.json` + untracked `handoff/`, `intelligence/`) + gitignored `.env`. Nothing to stash.

## State on handoff   ·  pre-filled — verify, don't retype
- Branch `main` (dirty), 0 ahead / 0 behind origin
- HEAD `25b306d`
- Recent commits:
```
25b306d chore(11-real-repo-triage-eval): settle phase
0ee3235 test(11-real-repo-triage-eval): prompt-parity check — authentic evidence reaches the model (AC-3)
ae3ac6e feat(11-real-repo-triage-eval): live accuracy gate on real corpus (T5)
7d73cb4 test(11-real-repo-triage-eval): CI corpus-integrity + scoring guard (T6)
e7791c8 feat(11-real-repo-triage-eval): add Runtime case; document 19-case deviation
edf25d0 feat(11-real-repo-triage-eval): real-repo labeled corpus from hono (T3)
a02fd86 feat(11-real-repo-triage-eval): diagnostic reporting + breakdown (T4)
8092fdc feat(11-real-repo-triage-eval): EvalCase gains provenance + rationale (T2)
```
- Loop: IDLE · phase 11-real-repo-triage-eval · tier (none)

## CADENCE context   ·  pre-filled from `cadence context handoff`
- Top recommendations:
  - (none)
- Open assumptions:
  - (none)
- Active decisions:
  - (none)
- Files in play:
  - (none)

## What landed this session
- `v1.0.0` tagged at the feature-complete MVP commit (first tag in the repo) and pushed.
- **Phase 10 — `extract-duplicate`** (necro refactor's 2nd type): `necro refactor --type extract-duplicate` lifts a clone group into one shared function + a call at each site, **code-not-diff**, multi-file splice/diff owned by necro, scratch-worktree verification, suggest-only. God-function path unchanged (dispatch by finding kind). 7 ACs PASS, live eval 10/10.
- **Phase 11 — `real-repo-triage-eval`**: T1 capture util (`src/triage/eval-capture.ts`), `EvalCase` gains provenance+rationale, **19-case hand-verified hono corpus** (`test/fixtures/triage-realrepo/` + `SOURCES.md`), diagnostic breakdown (`formatBreakdown`), live regression-floor gate, CI integrity test. 6 ACs PASS.
- Saved 2 findings to memory: `triage-realrepo-accuracy-baseline`, `necro-self-scan-degenerate`.

## Carry-forward gotchas
- **necro-on-itself is a degenerate scan target.** Its `package.json` points at `dist/` and there's no `src/index.ts`, so scanning `src/` resolves **zero** prod entries → all `maybe` findings carry identical, useless evidence (`0 production references`). Don't use necro-self as a triage corpus. (memory: `necro-self-scan-degenerate`)
- **LLM-edit features return CODE, not diffs** — still load-bearing across triage/refactor. (memory: `refactor-proposal-is-code-not-diff`)
- **Real-repo corpus = hono @`61d6d66`, 19 cases (5 dead / 14 alive)** in `test/fixtures/triage-realrepo/`. 13/14 alive cases carry *misleading* `0 static references` evidence — that's the deliberate false-positive trap; the persistent live failures are `RequiredRequestInit` + `detectResponseType` (live code flagged dead).
- **The real-repo live gate is a documented regression FLOOR** (precision≥0.4, recall≥0.3), **not** the 0.85 target. Model is non-deterministic (`thinking: adaptive`) — precision ranged 0.50–0.75 across runs. Calibration basis is in `SOURCES.md`.
- **Phase 11 deviation:** corpus is **19, not the planned ≥20** — one case (`cloneRawRequest`) was a genuinely-ambiguous documented-public-API-with-no-internal-caller and was excluded rather than coin-flipped. Documented in `SOURCES.md`.
- **Live evals need a key + a sourced `.env`** (runner does NOT auto-load): `set -a; . ./.env; set +a; npx vitest run test/triage-eval.live.test.ts`. There are now **4** live evals (triage synthetic + triage real-repo + refactor god-function + refactor extract-duplicate), all auto-skip without a key.
- **SDK isolation invariant holds:** `@anthropic-ai/sdk` is dynamic-`import()` only, reachable from `src/triage/` + `src/refactor/`; `cli`/`engine`/`fix` have no static import (asserted by tests). Keep new LLM code on the lazy path.
- **Headless CADENCE gates:** `draft approve … --no-approve`; `settle run --auto`; `draft check <path-to-DRAFT.md>`. The settle AC↔test gate needs each AC id (`AC-N`) in a test title (memory: `cadence-settle-ac-test-gate`).
- Dirty tree is **CADENCE bookkeeping only** — no WIP, no stash to restore.

## Next action
**Action:** Scope the next milestone — a **triage tuning phase** to raise `necro triage` precision toward ≥0.85 on the real corpus (phase 11 only *measured*; its boundary forbade tuning). Start with `cadence draft new 12-<slug> 12 --title=…`. Harness is ready: corpus `test/fixtures/triage-realrepo/cases.json`, `runEval`/`formatBreakdown` in `src/triage/eval.js`, live gate `test/triage-eval.live.test.ts`. Likely lever: revise the triage prompt so it stops over-trusting `0 static references` / dynamic-taint evidence; fix the two persistent false positives first — `RequiredRequestInit`, `detectResponseType`. (Alternative milestone if tuning is deferred: **refactor real-repo eval** — the refactor analog of phase 11.)

**Verify:** `cadence status` shows IDLE before drafting. After a tuning change, `set -a; . ./.env; set +a; npx vitest run test/triage-eval.live.test.ts -t "real-repo"` shows precision above the prior **0.50–0.75** baseline (run it 2–3× — the model is non-deterministic). `npx vitest run` green + `npm run typecheck` clean before any commit.

**If it fails:** triage tuning must NOT regress the synthetic eval or change scan/fix behavior, and must keep the **code-not-diff** + **lazy-SDK** invariants. Tag any new test `(AC-N)` up front (settle gate). Read the `claude-api` skill before touching model/prompt code. For a `website/` command on a Node-version error, `nvm use 22`.
