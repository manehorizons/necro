---
cadence_handoff: 1
generated_at: 2026-07-19T04:02:51.845Z
label: v1.4.0-published
loop_position: IDLE
active_phase: 55-library-export-surface
active_draft: 
tier: 
git_branch: main
git_dirty: true
git_head: 3ac9d33
git_ahead: 0
git_behind: 0
context_packet: .cadence/intelligence/context/handoff.json
---

# Session Handoff — 2026-07-19 (v1.4.0-published)

## TL;DR for the next session
- Resumed from the phase-55 handoff, then cut and published `@manehorizons/necro@1.4.0` to npm — the library export surface from phase 55 (`import { scan, explain, buildReachabilityModel, loadConfig, ... } from "@manehorizons/necro"`) is now live for consumers, closing the gap the last handoff flagged.
- Release went clean end-to-end: local clean-build + full suite (734/740) + typecheck + lint all green before tagging, then the `release.yml` CI job (guard → build → test → publish) passed on the first attempt — no retag needed this time (unlike v1.3.0's flaky-timeout retry).
- Loop is IDLE, no active draft, no blockers.
- Same two top-ranked unstarted recommendations as last handoff, still `needs-evidence`: `rec-20260701-016` (incremental symbol-graph cache) and `rec-20260718-001` (SKIP_DIRS silently skips any dir literally named `build`) — neither was touched this session.

## State on handoff   ·  pre-filled — verify, don't retype
- Branch `main` (dirty), 0 ahead / 0 behind origin
- HEAD `3ac9d33`
- Recent commits:
```
3ac9d33 chore: cut v1.4.0 — version bump, CHANGELOG finalized
0c5fc78 chore(cadence): stamp session handoff — phase-55-library-exports-shipped
e6f99a9 feat(55): library export surface — exports map + type declarations (rec-20260701-013)
05ef48f fix(test): raise timeout for cli-baseline's two-invocation tests
6ef2365 chore: cut v1.3.0 — version bump, CHANGELOG finalized
5e83c3a feat(54): toolchain hygiene bundle — Biome, Dependabot, CI matrix, self-scan gate (rec-20260701-015)
76f8377 chore(cadence): stamp session handoff — competitor-accuracy-shipped
64c7ae2 feat(53): knip/ts-prune competitor accuracy head-to-head (rec-20260701-012)
```
- Uncommitted (diff --stat):
```
.cadence/STATE.md   | 2 +-
 .cadence/state.json | 4 ++--
 2 files changed, 3 insertions(+), 3 deletions(-)
```
- Loop: IDLE · phase 55-library-export-surface · tier (none)

## CADENCE context   ·  pre-filled from `cadence context handoff`
- Top recommendations:
  - rec-20260701-016 — Incremental symbol-graph cache for large repos (candidate/needs-evidence)
  - rec-20260718-001 — SKIP_DIRS silently skips any directory literally named 'build' (candidate/needs-evidence)
- Open assumptions:
  - (none)
- Active decisions:
  - (none)
- Files in play:
  - `src/graph/symbol-graph.ts` — affected by rec-20260701-016 Incremental symbol-graph cache for large repos
  - `src/bench/` — affected by rec-20260701-016 Incremental symbol-graph cache for large repos
  - `src/discover.ts` — affected by rec-20260718-001 SKIP_DIRS silently skips any directory literally named 'build'

## What landed this session
- Added a dated `## [1.4.0] — 2026-07-19` CHANGELOG entry documenting the library entry point (`main`/`types`/`exports` map, named exports only, `tsc -p tsconfig.build.json` build step).
- Bumped `package.json`/`package-lock.json` to `1.4.0`, committed as `3ac9d33` (`chore: cut v1.4.0 — version bump, CHANGELOG finalized`), pushed to `origin/main`.
- Ran a clean `rm -rf dist && npm run build`, `npm test` (734 passed / 6 skipped), `npm run typecheck`, `npm run lint` — all clean before tagging.
- Tagged `v1.4.0` and pushed the tag (user explicitly confirmed first, since tag-push triggers the live public npm publish per `release.yml`).
- Watched the `Release` GitHub Actions run (`29671862346`) to completion — all steps green — then verified via `npm view @manehorizons/necro version` → `1.4.0`.

## Carry-forward gotchas
- **v1.4.0 is now live on npm with the library export surface** — the "don't assume npm consumers can `import` from the package yet" caveat from the previous handoff no longer applies. `import { scan, explain, buildReachabilityModel, loadConfig, ... } from "@manehorizons/necro"` resolves against the published tarball.
- The publish itself went clean this time (no retag) — the earlier v1.3.0 flaky-timeout issue (a test spawning 2 child CLI processes hitting vitest's 5s default) was already fixed (`05ef48f`) and did not recur.
- `.cadence/mcp-trust.json` stays untracked — don't commit it (excluded again this session).
- No merge/rebase in progress.
- No stash was created this session — nothing to restore on resume.

## Next action

No single forced next step — loop is IDLE with no active draft, and the release that was the prior handoff's open item is now done. The two live candidates are the same needs-evidence recommendations as last time.

**Action:** Run `cadence recommend` to confirm/pick the next phase from `rec-20260701-016` (incremental symbol-graph cache) or `rec-20260718-001` (SKIP_DIRS build-dir bug) — both currently `needs-evidence`, so expect a research/scoping step before either becomes `ready-for-milestone`. Alternatively, ask the user if there's a different priority before starting either.
**Verify:** n/a — this is a decision point, not a command to run blind.
**If it fails:** n/a.
