---
cadence_handoff: 1
generated_at: 2026-07-20T00:37:45.339Z
label: 
loop_position: IDLE
active_phase: 59-ts-export-quarantine
active_draft: 
tier: 
git_branch: main
git_dirty: true
git_head: 7631ffa
git_ahead: 0
git_behind: 0
context_packet: .cadence/intelligence/context/handoff.json
---

# Session Handoff — 2026-07-20

## TL;DR for the next session
- Shipped 4 phases this session: 56 (SKIP_DIRS Python `build`-dir bug), 57 (symbol-graph timing harness + evidence), 58 (symbol-graph cache v1 — whole-repo-unchanged, ~935x speedup on trpc), 59 (TS/JS `publicApiIds` export quarantine + truthful evidence). All committed and pushed to `origin/main` (HEAD `7631ffa`).
- Mid-session, ingested 8 new recommendations from a re-audit handoff doc (`docs/necro-reaudit-recommendations-handoff.md`, untracked, left as-is at the user's choice) — `rec-20260719-001` through `-008`. `-001` (the top-ranked one) is now shipped as phase 59; `-002` through `-008` are still open.
- Loop is IDLE, no active draft, no blockers.
- Top live candidates per `cadence recommend`: `rec-20260719-003` (TS plane symbol→file edges — `ready-for-milestone`, the natural next pick), `rec-20260719-005` (fix should skip Python certains, not silently no-op — `ready-for-milestone`), `rec-20260719-007` (Cobertura coverage.xml for Python — `ready-for-milestone`); `rec-20260719-002` and `-004` are `needs-decision` (design calls, not ready to draft yet).
- Last thing said to the user before this handoff: asked whether to keep going down the re-audit list (starting with `rec-20260719-003`) or take a break — no answer yet.

## State on handoff   ·  pre-filled — verify, don't retype
- Branch `main` (dirty), 0 ahead / 0 behind origin
- HEAD `7631ffa`
- Recent commits:
```
7631ffa feat(59): TS/JS export quarantine + truthful evidence (rec-20260719-001)
334cdb8 feat(58): symbol-graph cache v1 — whole-repo-unchanged (rec-20260701-016)
990aa54 docs: symbol-graph cache v1 implementation plan
332e6c8 docs: symbol-graph cache v1 design (rec-20260701-016)
e11926e feat(57): timing harness for buildSymbolGraph — evidence for rec-20260701-016
5e42c04 fix(56): scope SKIP_DIRS 'build' exclusion to non-Python configs (rec-20260718-001)
e39e9d7 chore(cadence): stamp session handoff — v1.4.0-published
3ac9d33 chore: cut v1.4.0 — version bump, CHANGELOG finalized
```
- Uncommitted (diff --stat):
```
.cadence/state.json | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)
```
- Loop: IDLE · phase 59-ts-export-quarantine · tier (none)

## CADENCE context   ·  pre-filled from `cadence context handoff`
- Top recommendations:
  - rec-20260719-003 — TS plane: add symbol→file edges so module-level refs and side-effect imports propagate (candidate/ready-for-milestone)
  - rec-20260719-005 — fix should explicitly count-and-skip Python certain findings, not silently no-op (candidate/ready-for-milestone)
  - rec-20260719-007 — Coverage: read Cobertura coverage.xml so the runtime-contradiction signal works for Python (candidate/ready-for-milestone)
  - rec-20260719-002 — Make fix --write verify-by-default; align README 'safe' language (candidate/needs-decision)
  - rec-20260719-004 — Invert taint: suppress certainty for dynamic-dispatch TARGETS, not the containing file (candidate/needs-decision)
- Open assumptions:
  - (none)
- Active decisions:
  - (none)
- Files in play:
  - `src/graph/python/symbol-graph.ts` — affected by rec-20260719-003 TS plane: add symbol→file edges so module-level refs and side-effect imports propagate
  - `src/fix/remove.ts` — affected by rec-20260719-005 fix should explicitly count-and-skip Python certain findings, not silently no-op
  - `src/analyze/coverage` — affected by rec-20260719-007 Coverage: read Cobertura coverage.xml so the runtime-contradiction signal works for Python
  - `README.md` — affected by rec-20260719-002 Make fix --write verify-by-default; align README 'safe' language
  - `src/analyze/reachability.ts` — affected by rec-20260719-004 Invert taint: suppress certainty for dynamic-dispatch TARGETS, not the containing file

## What landed this session
- Phase 56 (`5e42c04`): `SKIP_DIRS`'s unconditional `"build"` skip was silently dropping Python subpackages named `build` (confirmed on pip). Now only skipped when `config.include` has no Python glob.
- Phase 57 (`e11926e`): new `src/bench/symbol-graph-timing.ts` harness measured `buildSymbolGraph` against real checkouts (hono 378 files/4.4s, trpc 973 files/44.7s) — proved super-linear scaling, the evidence `rec-20260701-016` needed before being built.
- Design + plan docs (`332e6c8`, `990aa54`): `docs/superpowers/specs/2026-07-19-symbol-graph-cache-design.md` and `docs/superpowers/plans/2026-07-19-symbol-graph-cache-v1.md` — whole-repo-unchanged content-hash cache, deliberately scoped below full per-file incremental invalidation for v1 (follow-up rec if warranted later).
- Phase 58 (`334cdb8`): `src/graph/symbol-graph-cache.ts` (`buildSymbolGraphCached`, whole-repo-unchanged hit/miss on file-set + SHA-256 + config fingerprint + `necroVersion`), wired into `buildReachabilityModel`. Verified: trpc rebuild 50.5s → cached rebuild 54ms (~935x). Along the way fixed a `.gitignore` gap — `fp-realrepo.test.ts`/python-realrepo tests scan checked-in fixtures in place, so `**/.necro-cache/` needed adding alongside the existing `.bench-cache/` pattern.
- Ingested 8 recommendations (`rec-20260719-001..008`) from a re-audit handoff doc — one CLI schema-drift hit (`--priority P1/P2` doesn't exist; current schema is `low|medium|high|critical`, mapped P1→high/P2→medium per user decision).
- Phase 59 (`7631ffa`, SPEC-gated — first phase this session to go through `cadence spec new`/`spec approve` rather than straight to draft): `publicApiIds` was always empty for TS/JS (Python-only), so `deadEvidence`'s "not in package.json exports" line was fabricated for every TS finding. Added `isTsLibrary(ctx)` + `resolvePublicApiIds` (`src/graph/symbol-graph-public-api.ts`, resolves barrel/re-export chains via ts-morph's `getExportedDeclarations()`, matches ids to each declaration's own name not an export alias). `classify.ts` untouched — it already consumed `publicApiIds` correctly since phase 45's Python quarantine work. Verified live on necro's own repo: 24 public-API ids correctly resolved through `src/index.ts`'s barrel.
- Both `rec-20260701-016` and `rec-20260719-001` marked `shipped` in the CADENCE recommendation ledger with commit refs.

## Carry-forward gotchas
- `docs/necro-reaudit-recommendations-handoff.md` is untracked and not mine — it's the source doc the 8 `rec-20260719-*` recommendations were ingested from. Left as-is at the user's explicit choice; don't delete it without asking again.
- `.cadence/mcp-trust.json` stays untracked (established convention all session) — don't commit it.
- `cadence_spec_approve` needed a fresh `cadence mcp trust grant --tool cadence_spec_approve` mid-session (version mismatch: grant was for 1.46.0, running server was 1.47.0). If spec-approve refuses again with a version-mismatch error, re-grant the same way — it's a one-time per-version thing, not a real blocker.
- The symbol-graph cache (phase 58) writes `.necro-cache/` wherever a scan target lives, including nested inside `test/fixtures/` when tests scan those in place — this is now gitignored (`**/.necro-cache/`), so it's expected/harmless, not a sign of something broken if you see it appear during `npm test`.
- `rec-20260719-002` and `rec-20260719-004` are `needs-decision`, not `ready-for-milestone` — both need an actual design call from the user (verify-by-default check-set/perf-budget for `-002`; which taint-inversion strategy for `-004`) before they can be drafted, unlike `-003`/`-005`/`-007` which are ready to go straight to a DRAFT.
- No stash was created this session — nothing to restore on resume.

## Next action

No single forced next step — loop is IDLE, no active draft, no blockers. The open question I'd already put to the user (unanswered when `/handoff` was invoked): keep working down the re-audit recommendations, or stop here.

**Action:** Ask the user whether to continue with `rec-20260719-003` (TS plane: add symbol→file edges so module-level refs and side-effect imports propagate — `ready-for-milestone`, next natural pick, same phase/spec/draft/build/settle flow used for 56-59) or do something else. If continuing autonomously without the user weighing in, run `cadence recommend` to reconfirm ranking hasn't shifted, then `cadence recommendation promote rec-20260719-003 --status accepted --readiness ready-for-milestone` (mirrors how 56/57/58/59 were each promoted before drafting) → `cadence draft new <phase> <num> --from-rec rec-20260719-003 --title "..."` → fill Objective/AC/Tasks/Boundaries → `cadence draft approve` → build → settle → commit → push, same pattern as every phase this session.
**Verify:** n/a — this is a decision point, not a command to run blind.
**If it fails:** n/a.
