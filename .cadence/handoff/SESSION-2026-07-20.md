---
cadence_handoff: 1
generated_at: 2026-07-20T03:44:47.970Z
label: 
loop_position: IDLE
active_phase: 64-git-guard-cache-exclude
active_draft: 
tier: 
git_branch: main
git_dirty: true
git_head: 565a68a
git_ahead: 0
git_behind: 0
context_packet: .cadence/intelligence/context/handoff.json
---

# Session Handoff — 2026-07-20

## TL;DR for the next session
- 5 phases resolved this session (60, 62, 63, 64 shipped; 61 disproven and correctly not implemented), all committed and pushed to `origin/main`.
- Cut, tagged, and published **v1.5.0** to npm (confirmed live via `npm view @manehorizons/necro version` → `1.5.0`); the tag push hit a ~50min GitHub Actions partial outage that resolved on its own, no intervention needed.
- Synced the website docs — this session's changes plus several long-pre-existing stale claims (Python listed as "planned" when it's been shipped for many phases, a "Pre-1.0" homepage badge at v1.4.0+, "until the npm package ships" language, coverage described as lcov-only/"planned").
- Found and fully fixed CI-on-`main`, which had been red since the v1.4.0 cut and unnoticed: 3 separate root causes across 3 commits (typecheck-before-build step ordering, 4 latent lint errors, vitest timeout too tight under CI load). All 6 matrix jobs now pass.
- Loop is IDLE, no active draft, no blockers. Working tree is effectively clean (only cadence's own state files uncommitted).
- Remaining backlog — `rec-20260719-004`/`-006`/`-008` — all still need a human design decision or more evidence; none is `ready-for-milestone`. No unanswered question is currently pending with the user.

## State on handoff   ·  pre-filled — verify, don't retype
- Branch `main` (dirty), 0 ahead / 0 behind origin
- HEAD `565a68a`
- Recent commits:
```
565a68a fix(test): raise vitest default timeout to 20s — 10s still too tight
806d001 fix(test): raise vitest default timeout to 10s for loaded CI runners
bf69dbc fix(ci): typecheck after build; clear 4 latent lint format/import-order errors
76fc830 chore: cut v1.5.0 — version bump, CHANGELOG finalized
361c34c docs: sync website with phases 60-64 + long-stale Python/npm/coverage claims
901036b fix(64): dirty-tree guard ignores necro's own .necro-cache artifact (rec-20260720-001)
8ecf0c2 feat(63): fix --write verifies by default; --no-verify escape hatch (rec-20260719-002)
46fc148 feat(62): Cobertura coverage.xml reader for Python runtime-contradiction signal (rec-20260719-007)
```
- Uncommitted (diff --stat):
```
.cadence/STATE.md   | 2 +-
 .cadence/state.json | 4 ++--
 2 files changed, 3 insertions(+), 3 deletions(-)
```
- Loop: IDLE · phase 64-git-guard-cache-exclude · tier (none)

## CADENCE context   ·  pre-filled from `cadence context handoff`
- Top recommendations:
  - rec-20260719-004 — Invert taint: suppress certainty for dynamic-dispatch TARGETS, not the containing file (accepted/needs-decision)
  - rec-20260719-006 — DEFAULT_CHECKS is npm-only; adapt or refuse for Python/mixed repos (candidate/needs-decision)
  - rec-20260719-008 — Demote certain→likely for symbols whose initializer has observable side effects (candidate/needs-evidence)
- Open assumptions:
  - (none)
- Active decisions:
  - (none)
- Files in play:
  - `src/analyze/reachability.ts` — affected by rec-20260719-004 Invert taint: suppress certainty for dynamic-dispatch TARGETS, not the containing file
  - `src/refactor/index.ts` — affected by rec-20260719-006 DEFAULT_CHECKS is npm-only; adapt or refuse for Python/mixed repos
  - `src/fix/remove.ts` — affected by rec-20260719-008 Demote certain→likely for symbols whose initializer has observable side effects

## What landed this session
- `rec-20260719-003` → phase 60: TS-plane symbol→file edges (module-level top-level refs + bare side-effect imports now propagate reachability).
- `rec-20260719-005` → phase 61: **disproven, not implemented** — `classify.ts`'s AC-6 (phase 45) already hard-caps every Python finding away from `certain`, so the bug the recommendation described can't occur; settled with FAIL verdicts + empirical evidence so it doesn't resurface.
- `rec-20260719-007` → phase 62: Cobertura `coverage.xml` reader (`pythonCoveragePath` config key, merges with lcov).
- `rec-20260719-002` → phase 63: `fix --write` now verifies (typecheck-only) by default; `--no-verify` escape hatch. Discovered and filed `rec-20260720-001` while testing this.
- `rec-20260720-001` → phase 64: the dirty-tree guard now excludes `.necro-cache/` via a git pathspec magic exclude (it was seeing necro's own scan-time cache write as an uncommitted change).
- Website docs synced (see TL;DR) — `npm run build` in `website/` (Node 22) verified clean, all internal links valid.
- **v1.5.0 cut, tagged (`v1.5.0`), and published to npm** — CHANGELOG entry added, `package.json`/`package-lock.json` bumped, `git tag v1.5.0 && git push origin v1.5.0` triggered the `release.yml` workflow, which completed successfully (build → test → `npm publish --provenance`).
- **CI-on-`main` fixed** (was red since the v1.4.0 cut, three separate causes, one commit each):
  1. `bf69dbc` — `ci.yml` ran `Typecheck` before `Build`, but `test/library-exports.test.ts` imports `@manehorizons/necro` by its own name, which only resolves via the exports map once `dist/` exists → reordered to Lint → Build → Typecheck → Test. Also cleared 4 pre-existing biome format/import-order errors that were separately failing the `Lint` step.
  2. `806d001` — bumped vitest's global `testTimeout` 5s → 10s (loaded CI runner + coverage instrumentation raced the default; same class of issue already hit once before in `test/cli-baseline.test.ts`).
  3. `565a68a` — 10s still wasn't enough on one ubuntu-latest/node-22+24 run (whole `test/fix.test.ts` file ran 10-18x slower than local that run); raised to 20s, clearing the observed 14.9s worst case with margin.
  All 6 matrix jobs (ubuntu/macos × node 20/22/24) confirmed green after the third fix.

## Carry-forward gotchas
- `docs/necro-reaudit-recommendations-handoff.md` stays untracked and not mine — left as-is at the user's explicit choice, carried over from before this session; don't delete without asking again.
- `.cadence/mcp-trust.json` stays untracked (established convention all session) — don't commit it.
- The only three live recommendations (`rec-20260719-004`, `-006`, `-008`) all need a human design call or more evidence before they can be drafted — none is `ready-for-milestone`. See the CADENCE context block above for what each one is asking.
- vitest's global `testTimeout` is now 20s (was the 5s default) — deliberate, after twice observing real 10-18x CI-runner slowdowns for ts-morph-heavy tests, not an oversight. A genuinely-hung test in dev now takes 20s to fail instead of 5s.
- `release.yml` (the actual npm-publish workflow) does **not** run a `tsc --noEmit` typecheck step — only `ci.yml` does. That's why the typecheck-before-build ordering bug never blocked a real release, only the main-branch CI signal; don't assume a green release run means CI/ci.yml would also be green.
- No stash was created this session — nothing to restore on resume.

## Next action

No single forced next step — loop is IDLE, no active draft, no blockers, CI is green, v1.5.0 is live. The one open item I haven't yet put to the user: which of the three remaining recommendations to decide next, or whether to stop here.

**Action:** Ask the user whether to continue deciding the remaining re-audit recommendations — `rec-20260719-004` (invert taint to target dynamic-dispatch symbols, not containing files; highest priority, 3 composable design options, will take some back-and-forth), `rec-20260719-006` (DEFAULT_CHECKS is npm-only — simpler decision, what should verify-removal/refactor's defaults do for Python/mixed repos), or `rec-20260719-008` (needs more evidence before it's even decision-ready) — or move on to something else entirely.
**Verify:** n/a — this is a decision point, not a command to run blind.
**If it fails:** n/a.
